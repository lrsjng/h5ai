extends ./page.tpl.jade

block init

    - var title = pkg.name + ' info page - v' + pkg.version
    - var module = 'info'

block body

    div#content
        h1#header
            a(href='#{pkg.homepage}') #{pkg.name}
